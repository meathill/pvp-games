{
  "name": "pvp-games",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for PVP game server, games, and shared utilities.",
  "license": "MIT",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -C packages/web dev",
    "dev:server": "pnpm -C packages/server dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "biome format --write"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
